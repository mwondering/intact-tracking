# DR-center 新调参准备：权重 0.2、scale 0.2

**后续状态：** 用户已确认“同 DR、不同 motion”，并明确正样本权重为 **0.1**。实现、真实 checkpoint 梯度检查及 58 项测试通过，已从 u10338 启动八卡无限续训。最终配置与启动验证见 [正式续训记录](memory350_dr_center_weight02_scale02_positive01_20260915.md)。下文保留早期澄清前的准备过程，0.001 不再是实际训练值。

用户要求将中心关系权重改为 0.2、DR 距离映射 scale 改为 0.2，并新增正样本后无限续训。第三点原文为“不同 dr 环境同 motion 的正样本约束”，与上一轮讨论的“同 DR、不同 motion”方向相反。已于本轮向用户发起配对方向确认；在确认前不接入第三项、不启动只有前两项变化的训练。原八卡任务继续运行。

已完成前两项所需的恢复支持：`--retune-representation-weight` 与新增 `--retune-dr-distance-scale` 必须各自显式开启，且要求新恢复阶段。其他损失、物理参数 schema、archive 和数据约束仍严格校验。新阶段分别记录旧/新权重和旧/新 scale。

42 项相关测试通过，包括双参数显式授权、其他损失不可改变、父配置不被改写、无限更新和学习率下限。`git diff --check` 通过。

使用真实 u8800 checkpoint 和 rank 0 固定验证 batch 的前 64 条样本，在 GPU 6 上检查两种损失的前向/反向；未执行 optimizer step：

| 指标 | 权重 0.04 / scale 0.3 | 权重 0.2 / scale 0.2 |
|---|---:|---:|
| Prediction loss | 0.10321437 | 0.10321437 |
| 中心关系损失 | 0.01002748 | 0.06323205 |
| 加权中心关系损失 | 0.00040110 | 0.01264641 |
| Encoder 梯度范数 | 0.431322 | 0.488008 |
| 有效中心对 | 861 | 861 |

两组损失、全部模型梯度均有限。以上仅验证前两项参数变化的计算链路，不是新版训练效果，也没有加入正样本。

准备记录位于 `runs/limb_context_20260915_dr_center_tuned_weight02_scale02/preparation/`，包含源代码快照、原进程身份、准备合同和 `weight_scale_gradient_preflight.json`。确认第三项后，还需实现配对损失、验证梯度方向和实际配对有效性，协调保存旧任务，再从保存点恢复八卡无限训练。未另行指定时，正样本系数拟采用上轮建议的 0.001；最终以用户确认的配对方向和后续指示为准。
