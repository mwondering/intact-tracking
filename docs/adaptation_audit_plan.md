# 2026-09-07 对照审计与第二阶段继续计划

01:23 UTC 用户新增硬约束：原始环境奖励完全固定。已停止所有仍在运行的
改奖励分支；既有特权 teacher 收益混入奖励修改，不能作为固定奖励证据。
原始奖励 nominal/DR 配对训练继续。新 teacher 从共同原始 tracker 初始化，
对比输入/架构；仅使用固定奖励谱系 teacher 进行下一阶段蒸馏。
奖励项/函数源码/权重/参数/dt 签名写入新模型，旧改奖励模型只作历史评测。

用户新增要求：仅改变训练是否使用 DR，在相同 nominal 测试环境比较；
复核 DR 特权 policy；排查特权高度/接触带来的收益；采用蒸馏继续第二阶段；
不接受用平均 tracking 改善掩盖失败率退化。持续至第二阶段完成或用户询问进度。

## 已冻结的评测矩阵

`runs/adaptation_goal/eval_v2/audit_910xx/freeze.json` 在所有完整评测之前冻结。
91001/91002/91003 为审计/开发种子，不消耗第二阶段最终 600xx 确认种子。
每种子全部 42 个动作 × 8 起点，最长 500 步，v2 隔离 reset 协议。

6 个固定模型各在 nominal 与原 DR+payload 下评测：冻结 tracker、固定
nominal 残差基线、旧配对 nominal1750、旧配对 DR1750、clean teacher500、
可部署 rotation350 student。额外对 clean teacher 做三种消融，各测两种
环境：替换高度、接触、二者同时替换。总计 54 次完整评测。

高度/接触消融同时覆盖 tracking 前端与特权 latent 的输入，使用原可部署
估计器的预测替代真值；不是把输入置零。单元测试将两处真值置 NaN，确认
选择的特权不再影响输出，其他特权不变。它回答已训练策略的依赖性，不能
直接等价于“去掉该特权重新训练”的因果收益，因此必要时增加重训消融。

新增审计数据只用于评测记录：每世界实际物理参数指纹与各项失败终止条件。
绝不进入 student 动作通路。DR 范围、观测噪声、动作尺度、力矩上限、
评测起点规则和困难动作集合均不改变。

## 配对训练的解释范围

旧 seed42 两实验的保存参数仅 physics/output_dir 不同，actor 架构相同，
都使用相同冻结 tracker；使用共同 checkpoint1750，避免比较不同预算。
checkpoint0 已经执行过第 0 轮更新，不是可直接验证 bitwise 初始化的文件。
旧对照为单训练种子，不能将评测种子当成独立训练重复。

补充配对训练将保存真正更新前的 checkpoint_initial；在环境创建后单独
设定模型构造与 rollout RNG，逐 tensor 核对 actor/critic 初始状态一致。
共享同一个已有 DR 预训练 tracker，比较的是后续训练 DR 开关的影响，
不是声称 nominal 分支整个生命周期从未经历 DR。

## 失败率约束与第二阶段

先对同种子、同起点、同物理指纹的冻结 tracker 与残差模型比较失败数、
新出现/被修复的失败片段、终止类型。另与 nominal 目标基线对比。此前
跨 nominal/DR 环境的 5 对 1 不足以证明残差比同环境冻结 tracker 更差。
但它确实意味着不能声称失败风险等同，此前 +1pp 容忍线不再作为通过依据。

后续候选要求在冻结的验收集合中不出现相对目标基线/同环境冻结 tracker
的观察失败率退化，并报告配对区间与样本量限制；不宣称有限测试保证零风险。
保持全部 10 项 tracking 标准，第二阶段不可保留任何特权 actor 输入。
蒸馏使用 teacher 在 student 访问状态上的动作标签，标签仅进入训练损失；
若剩余差距来自估计前端，需要一起学习可部署替代，不只拟合环境 latent。

两组已停训练：rotation2/4 seed75，均保留至 checkpoint1000 和所有早期模型。
未部署/未使用尚未验证的 static correction 模块；优先审计与必要的蒸馏实验。
